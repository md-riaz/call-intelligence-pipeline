# samples/

Put your own audio files here to try the pipeline:

```bash
transcribe-check --file samples/your-call.wav
```

Audio files in this folder are **git-ignored on purpose** — call recordings
often contain personal data (names, phone numbers, account details). Do not
commit real recordings to a public repository.
